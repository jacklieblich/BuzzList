<a href=<%=list_path(list.id)%> class="list-group-item">
  <span class="badge"><%= pluralize(list.list_episodes.count, "Episode") %></span>
  <div class = "row">
    <div class = "col-md-4">
      <%= image_tag(list.show.poster, size: "100")%>
    </div>
    <div class = "col-md-4">
      <h2><%= Show.find_by(id: list.show_id).title %></h2>
    </div>
  </div>
</a>
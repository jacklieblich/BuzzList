<% if activity.trackable.likable_type == "Clip" %>
  <% episode = Clip.find(activity.trackable.likable_id).episode %>
  <% else %>
    <% episode = Quote.find(activity.trackable.likable_id).episode %>
<% end %>
liked a <%= link_to activity.trackable.likable_type.downcase, show_path(episode.season.show, items: "media", anchor: activity.trackable.likable_type.downcase + "_" + activity.trackable.id.to_s) %> from <%= link_to episode.title, episode %>
